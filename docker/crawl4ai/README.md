# Notes

- Generate CRAWL4AI_API_TOKEN token

```bash
openssl rand -hex 32
```

- Use CRAWL4AI_API_TOKEN for mcp server bearer token
- MCP server endpoint: `http://localhost:11235/mcp/sse` (sse)
- Streamable http is not implemented yet
